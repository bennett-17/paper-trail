# Copy this file to .env and fill in your details:
#   cp .env.example .env
#
# paper-trail loads .env automatically from the working directory at
# startup (internal/envfile) — no third-party dependency, and any value
# already exported in your real shell environment always wins over this
# file.

# Required for lookup/filings/graph/fulltext. SEC EDGAR requires a
# descriptive User-Agent (name + contact email) on every request.
# See https://www.sec.gov/os/accessing-edgar-data
EDGAR_USER_AGENT="Your Name your.email@example.com"

# Required for the ukcharity command only -- the Charity Commission for
# England and Wales's API has no keyless option, unlike every other
# source this tool uses. Register for a free account and subscribe to
# the "Register of Charities" product at
# https://api-portal.charitycommission.gov.uk, then copy both keys from
# your subscription's page (click "Show" next to each). The primary key
# is used first on every request; the secondary is only used as an
# automatic fallback if the primary is rejected (e.g. mid-rotation, or
# it was regenerated and this file hasn't caught up yet) -- set at least
# the primary, the secondary is optional but recommended.
# UK_CHARITY_API_KEY_PRIMARY=
# UK_CHARITY_API_KEY_SECONDARY=

# Required for the sanctions command only -- Trade.gov's Consolidated
# Screening List API (OFAC SDN + State/BIS restricted-party lists),
# same Azure API Management setup as the UK Charity Commission above
# (primary/secondary key pair, primary tried first). Sign up for a
# free account at https://developer.trade.gov, subscribe to "Data
# Services Platform APIs" under Products, then copy both keys from your
# Profile page.
# CSL_API_KEY_PRIMARY=
# CSL_API_KEY_SECONDARY=

# Required for the companieshouse command, and used by risk for UK
# charities that are also registered companies (director/PSC/charges
# data, via the CompaniesHouseNumber already returned by the Charity
# Commission API). Sign up for a free account at
# https://developer.company-information.service.gov.uk, create an
# application, and request a REST key (not Web or Streaming) -- unlike
# the other UK/US integrations here, this is a single key, no
# primary/secondary rotation pair.
# COMPANIES_HOUSE_API_KEY=

# Used by risk's US SAM.gov Exclusions screen (federal contractor/
# individual debarment list) -- register a free account at
# https://sam.gov, go to your Account Details page, and request a
# public API key (visible immediately, but only shown once -- copy it
# somewhere safe before navigating away). The public tier is 10
# requests/day; registering a business entity and requesting the
# elevated "Data Entry" role raises that to 1,000/day, but isn't
# required just to use this.
# SAM_GOV_API_KEY=

# Used by the nzbn command, and by risk's New Zealand Business Number
# (NZBN) entity search/officer fan-out. Register a free account at
# https://portal.api.business.govt.nz and subscribe to BOTH the "NZBN"
# and "Companies Entity Role Search" API products (they're separate
# products under one account) -- approval is a manual review, not
# instant self-serve, but the portal's own stated SLA is one working
# day once you've signed their API Access Agreement.
# NZBN_API_KEY=
# Only set this if your account issues a different subscription key
# for the Companies Entity Role Search product than for NZBN itself --
# NZBN_API_KEY is used for both by default.
# NZBN_ENTITY_ROLE_API_KEY=

# Reserved for Phase 2 (OpenCorporates integration). Not read yet.
# OPENCORPORATES_API_KEY=

# Optional for risk's OpenFEC (FEC campaign contribution) screen --
# works with no registration at all via FEC's public shared DEMO_KEY,
# but that pool is rate-limited across every unregistered caller. Get
# your own free key instantly (no approval wait) at
# https://api.data.gov/signup/?api=fec for a much higher personal rate
# limit.
# OPENFEC_API_KEY=
